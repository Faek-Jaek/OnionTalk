const bip39 = require('bip39'); // Import the packaged package
const eccrypto = require('eccrypto') // Import the packaged package
window.bip39 = bip39; // Expose bip39 globally
window.eccrypto = eccrypto; // Expose eccrypto package globally 